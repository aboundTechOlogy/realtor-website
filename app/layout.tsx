import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "John Smith Realty - 20 Years of Market Expertise",
  description: "Experienced real estate agent with 20 years navigating every market condition. Honest advice, strategic pricing, proven results.",
  keywords: "real estate, realtor, home selling, market analysis, strategic pricing",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
